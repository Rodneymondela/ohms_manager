# Example repository. Centralize database reads/writes & complex queries.
# from ..extensions import db
# from ..models import YourModel
#
# def get_items(limit=50, offset=0):
#     return YourModel.query.limit(limit).offset(offset).all()
